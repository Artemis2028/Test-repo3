import SwiftUI

/// MGRS GPS / GridFix — iOS port shell (bundle id: app.gridfix.ios).
@main
struct GridFixApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
